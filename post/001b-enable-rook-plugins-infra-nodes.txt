

# Update

oc edit configmap rook-ceph-operator-config -n openshift-storage

# Then add taints "operators: Exists" so that they will be running all nodes including master nodes. 
data:
  CSI_LOG_LEVEL: '5'
  CSI_PLUGIN_TOLERATIONS: |
      operator: Exists
    - key: node.ocs.openshift.io/storage
      operator: Equal
      value: "true"
      effect: NoSchedule
